apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # TODO: Pin this to a specific Istio version when you choose one.
  # Example (you will replace this placeholder with the real URL):
  # - https://raw.githubusercontent.com/istio/istio/release-1.22/manifests/profiles/default.yaml
  - <ISTIO_DEFAULT_PROFILE_URL>
